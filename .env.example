# FortiGate Sniffer to PCAP Converter - Environment Configuration
# Copy this file to .env and update the values

# REQUIRED: Generate a secure random key (minimum 32 characters)
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secure-secret-key-here-at-least-32-chars

# Environment: "development" or "production"
# - development: enables debug mode, SQL logging, CORS for localhost, OpenAPI docs
# - production: disables debug features, enables HSTS, restricts CORS
ENVIRONMENT=development

# Debug mode (true/false)
# Enables SQL logging and verbose error messages
DEBUG=false

# Database URL (SQLite default, can use PostgreSQL in production)
# Default: sqlite:///./database.db
# Docker: sqlite:////app/data/database.db
# PostgreSQL: postgresql://user:password@localhost/dbname
# DATABASE_URL=sqlite:///./database.db
